# 끝말 잇기 — V1 디자인 목업 인수인계

다른 세션에서 이 디자인 작업을 이어가기 위한 문서.

## 현재 상태 (2026-06-26)

**v1 = 탭 → 큰 드롭존 채우기 + 더미 6라운드.** 보기 카드를 탭하면 그 카드가 중앙의 큰
드롭존으로 날아가 안착하고, 미리 짜둔 더미 시나리오의 다음 라운드로 진행한다. 6라운드 후
간단한 완주 상태("다 이었어요! 🎉").
설계 스펙: `docs/superpowers/specs/2026-06-26-wordchain-v1-tap-dropzone-multiround-design.md`

## 아티팩트 (현재 배포본)

- **URL:** https://claude.ai/code/artifact/d6c42493-5203-4cfe-8355-b2816270456c
- 본인(계정) 소유 · 기본 비공개. 다른 세션에서도 이 URL로 열림.
- **같은 URL에 이어서 덮어쓰려면:** 새 세션에서 이 URL을 붙여넣고 "이 아티팩트 이어서 작업하자"라고 하면 됨.
  Claude가 `Artifact` 도구에 `url`을 넘겨 **같은 URL로 재배포**한다. (url 안 넘기면 새 URL이 생기니 주의.)

## 이 폴더 구성

| 파일 | 내용 |
|---|---|
| `mockup.html` | 현재 배포된 자기완결 목업(폰트·그림 base64 + 시나리오·인터랙션 JS 내장). **직접 편집 금지** — 항상 build.py로 생성. |
| `build.py` | `assets.json`을 읽어 `mockup.html`을 재생성. **디자인·시나리오·인터랙션 수정은 전부 여기서.** `python3 build.py`. |
| `assets.json` | 폰트(Jua/Gowun woff2) + 픽토그램 22개 base64. 새 단어 그림이 필요하면 여기에 추가(아래 참고). |

## 재생성 → 재배포 순서 (새 세션)

1. `python3 frontend/v1/mockup/build.py` 로 `mockup.html` 갱신.
2. `Artifact` 도구로 `mockup.html`을 배포하되 위 URL을 `url`로 넘겨 같은 곳에 덮어쓰기.

### 검증 메모
- 이 환경엔 헤드리스 브라우저/jsdom이 없음 → 빌드 후 정적 검증만 가능(`node --check`로 JS 문법,
  f-string 누수 `{{`/`}}` 0 확인). **실제 동작·손맛 확인은 아티팩트를 브라우저에서 열어서** 함.

## v1 인터랙션 (build.py 안에 구현됨)

- **시나리오:** `SCENARIO` 배열(라운드별 `now`/`tail`/`options`/`answer`). 한 라운드 모양을
  **엔진 `toView` 출력과 정렬**해 둠 → 엔진 통합 때 이 배열만 엔진 출력으로 교체.
- **탭 동작:** 보기 카드 클릭 → 원본 카드를 transform으로 드롭존까지 이동·스케일(~0.38s) →
  드롭존을 정답 카드로 채움(초록 테두리, pop) → ~0.48s 후 다음 라운드 렌더 + 진행도 점 채움.
- **reduced-motion:** 날아가는 애니메이션 생략, 즉시 채움.
- **오답 처리 없음:** 어느 보기를 눌러도 진행. 다음 "방금 만든 말"은 정해둔 정답 체인을 따라감(스펙대로).
- **마이크 카드:** 표시만, 탭해도 no-op.

### 더미 체인 (자동 큐레이션·그림 보유 검증됨)
간호사 → 사과 → 과자 → 자전거 → 거북이 → 이야기 → 기차 (6라운드)

## 확정된 디자인 결정 (V1)

- **대상:** 특수교육 아동 / **태블릿 가로**. **메인 기기 = 갤럭시 탭 A11+ (1920×1200, 16:10)**. 4:3·16:9도 안 깨지게.
- **톤:** 차분·저자극. 흰 카드 + 파스텔 배경(하늘→크림). **강조색은 의미 있는 곳에만** —
  amber `#EE9740`=연결 글자(끝말)/빈 드롭존, green `#54B98A`=진행/채워진 카드/완주.
- **폰트:** Jua(둥근 디스플레이=단어·프롬프트), Gowun Dodum(UI). 오프라인용 TTF: `frontend/v1/font/`.
- **레이아웃(플레이 화면):**
  - 상단: 브랜드(좌) · **점-대시 진행도(중앙, done●/현재◎/남은—)** · 다시듣기 🔊(우).
  - 중앙(히어로): `[방금 만든 말 카드] ─(공유 글자)─ [큰 빈 드롭존]`. **드롭존이 주인공**(보기 카드와 같은 크기).
  - 하단: **보기 카드 3개(골드 테두리) + 슬림 마이크 카드(블루)** = 4칸. 보기 카드에 `:active` 눌림 피드백 있음.
- **반응형:** 컨테이너 기준 단위(`cqmin/cqw/cqh`)로 스케일, 스크롤 없이 한 화면.

## 새 단어 그림 추가하는 법

`build.py`의 `SCENARIO`에 새 단어를 쓰려면 그 단어가 `assets.json["assets"]`에 있어야 함.
없으면: `frontend/v1/tg_words.json`에서 단어의 `pic` id를 찾아 `frontend/v1/img/<pic>.png`를
base64로 인코딩해 `assets.json`에 `{"단어": {"pic":id, "uri":"data:image/png;base64,..."}}` 추가.
(이전에 14개를 그렇게 병합함 — 같은 패턴.)

## 단어/그림 자산 현황

- 그림 출처: **ARASAAC** 단일 소스(픽토그램), 한국어 키워드 첫 결과 채택 → 동음이의어 오매칭 가능(검수 필요).
- `docs/특교 끝말잇기 단어 셋.rtf`(382개) 기준 → 그림 보유 **353개** = `frontend/v1/tg_words.json`. 픽토그램은 `frontend/v1/img/`.
- 데모 단어 최종 큐레이션은 미확정. 고르는 도구: `frontend/v1/pick.html`(로컬 서버로 열기).

## 다음 단계 / 미정

- **v2: 드래그앤드롭** 입력(카드를 직접 끌어 드롭존에). v1과 같은 드롭존 무대 공유.
- **엔진 연결:** `SCENARIO`를 엔진 `toView` 출력으로 교체 + 엔진 JS·단어 데이터 인라인.
  - 진짜 프론트(`frontend/index.html`+`app.js`+`backend/kkutu-engine.js`)는 외부 `fetch`/외부 script라
    아티팩트 CSP에 그대로는 못 올림 → build에서 전부 단일 HTML로 인라인해야 함.
  - 엔진의 "안전 정답 최소 2개" → "항상 보기 정확히 3개" 보장하려면 v1 전용 엔진 조정 필요(미구현).
- **목업 ↔ 진짜 프론트 수렴:** 진짜 프론트가 목업 비주얼을 입고, 둘을 하나의 코드베이스로.
- **완주(전체 체인 회고) 화면** 디자인.
- 데모 단어 세트 최종 확정 / 카드 선택→연결 애니메이션 추가 다듬기.
- 미반영 레퍼런스: 중앙 원형 스피커, 하단 트레이/선반, 안내 마스코트.
